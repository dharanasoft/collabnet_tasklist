<h1>Completed tasks</h1>
<ul class="tasks">
<% @completed.each do |t| %>
  <li>
    <%=link_to t.title, t%>
    <%=link_to 'edit', edit_task_path(t) %>
    <%=link_to 'delete', task_path(t), :method=>'delete', :confirm=>'Are you sure you want to delete it?'%>
  </li>
<% end %>
</ul>
